% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carryover_models.R
\name{apply_carryover_to_component}
\alias{apply_carryover_to_component}
\title{Apply carryover decay to a bio-response mean trajectory}
\usage{
apply_carryover_to_component(
  component_means,
  trial_data,
  halflife,
  form = "exponential",
  shape = 1,
  power = 1.5
)
}
\arguments{
\item{component_means}{Numeric vector of component means per
timepoint (bio-response component).}

\item{trial_data}{Trial design tibble augmented by
\code{\link[=prepare_trial_data]{prepare_trial_data()}} with \code{on_drug} and \code{tsd} columns.}

\item{halflife}{Carryover half-life (weeks). \code{NULL} or \code{0} leaves
\code{component_means} unchanged.}

\item{form}{Decay form passed to \code{\link[=carryover_decay]{carryover_decay()}}.}

\item{shape}{Weibull shape parameter; see \code{\link[=carryover_decay]{carryover_decay()}}.}

\item{power}{Power exponent; see \code{\link[=carryover_decay]{carryover_decay()}}.}
}
\value{
Numeric vector of adjusted component means.
}
\description{
Hendrickson-style carryover: at each off-drug timepoint, add the
previous timepoint's bio-response mean scaled by the decay
multiplier from \code{\link[=carryover_decay]{carryover_decay()}}. Carryover is applied only to
the bio-response (br) component and only at timepoints with
\code{tsd > 0} and \code{on_drug == FALSE}.
}
