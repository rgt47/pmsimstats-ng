% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trial_design.R
\name{build_trial_design}
\alias{build_trial_design}
\title{Construct a trial-design object as a list of per-path tibbles}
\usage{
build_trial_design(
  name_longform = "Trial Design 1",
  name_shortform = name_longform,
  timepoints,
  timeptnames = paste0("V", seq_along(timepoints)),
  expectancies,
  ondrug
)
}
\arguments{
\item{name_longform}{Human-readable design name.}

\item{name_shortform}{Short tag for plots and filenames.}

\item{timepoints}{Numeric vector of visit weeks (cumulative).}

\item{timeptnames}{Character vector of visit labels. Default
\code{paste0("V", seq_along(timepoints))}.}

\item{expectancies}{Numeric vector of per-visit expectancy values
(same length as \code{timepoints}).}

\item{ondrug}{List of binary vectors (same length as \code{timepoints})
indicating on-drug status per path. One element per trial path.}
}
\value{
A list with elements \code{metadata} (design name, timepoints,
expectancies, ondrug) and \code{trialpaths} (a list of per-path
tibbles).
}
\description{
Returns a list \code{list(metadata, trialpaths)} where \code{trialpaths} is
a list of per-path tibbles with one row per timepoint and columns
\code{timeptnames}, \code{t_wk}, \code{e} (expectancy), \code{tod} (cumulative time on
drug), \code{tsd} (cumulative time since discontinuation), and \code{tpb}
(cumulative time with positive expectancy). Direct port of the
pmsimstats-ng tidyverse \code{build_trial_design}.
}
\examples{
td <- build_trial_design(
  name_longform = "Hybrid",
  name_shortform = "H",
  timepoints = c(2, 4, 6, 8),
  expectancies = c(0.5, 0.5, 0, 0),
  ondrug = list(c(1, 1, 0, 0), c(0, 0, 1, 1))
)
}
