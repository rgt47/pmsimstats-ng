% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data.R
\name{generate_data}
\alias{generate_data}
\title{Generate simulated N-of-1 trial data}
\usage{
generate_data(
  model_param,
  resp_param,
  baseline_param,
  trial_design,
  empirical,
  make_positive_definite,
  seed = NA,
  lambda_cor = NA,
  verbose = FALSE,
  track_pd_stats = TRUE,
  cached_sigma = NULL,
  dgp_architecture = "mvn"
)
}
\arguments{
\item{model_param}{Model-parameter tibble (single row) with
elements including \code{N}, \code{c.bm}, \code{c.tv}, \code{c.pb}, \code{c.br},
\code{c.cf1t}, \code{c.cfct}, \code{carryover_t1half}.}

\item{resp_param}{Response-parameter tibble with columns \code{cat}
(in \verb{\{"tv","pb","br"\}}), \code{max}, \code{disp}, \code{rate}, \code{sd}.}

\item{baseline_param}{Baseline-parameter tibble with columns \code{cat}
(in \verb{\{"bm","BL"\}}), \code{m}, \code{sd}.}

\item{trial_design}{Single-path trial-design tibble with columns
\code{timepoint_name} (or \code{timeptnames}), \code{t_wk}, \code{e}, \code{tod}, \code{tsd},
\code{tpb}.}

\item{empirical}{Logical; \code{TRUE} forces the sample correlation to
exactly match the target. Typically \code{FALSE}.}

\item{make_positive_definite}{Logical; force PD correction when
covariance is non-PD. Usually \code{TRUE}.}

\item{seed}{Randomization seed or \code{NA}.}

\item{lambda_cor}{BM-BR correlation decay rate. \code{NA} auto-derives.}

\item{verbose}{Logical; emit diagnostic messages.}

\item{track_pd_stats}{Logical; attach PD-correction attributes to
the returned tibble (\code{sigma_count},
\code{non_positive_definite_count}, \code{non_positive_definite_rate}).}

\item{cached_sigma}{Pre-built Sigma list from
\code{\link[=build_sigma_matrix]{build_sigma_matrix()}} to skip construction.}

\item{dgp_architecture}{Either \code{"mvn"} (Architecture B, default)
or \code{"mean_moderation"} (Architecture A).}
}
\value{
A tibble with \code{bm}, \code{BL}, one column per factor-timepoint
pair (\verb{<tp>.tv}, \verb{<tp>.pb}, \verb{<tp>.br}), \code{ptID}, delta columns
\verb{D_<tp>}, and symptom columns \verb{<tp>}.
}
\description{
Core data-generating function for the simulation pipeline. Samples
participant data from a multivariate normal with covariance
constructed by \code{\link[=build_sigma_matrix]{build_sigma_matrix()}}. Under Architecture A
(\code{dgp_architecture = "mean_moderation"}), applies an additive
biomarker-scaled shift to the bio-response columns at on-drug
timepoints. Under Architecture B (\code{"mvn"}, default), the
biomarker-response interaction is encoded via differential
correlation in the covariance matrix.
}
