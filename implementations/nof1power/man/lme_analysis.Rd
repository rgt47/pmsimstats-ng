% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{lme_analysis}
\alias{lme_analysis}
\title{Linear mixed-effects analysis of a single simulated trial}
\usage{
lme_analysis(trial_design_set, dat, op)
}
\arguments{
\item{trial_design_set}{A list of per-path tibbles with
timepoint/time/expectancy/drug columns (the \code{trialpaths}
element of a \code{\link[=build_trial_design]{build_trial_design()}} result).}

\item{dat}{Simulated data tibble produced by \code{\link[=generate_data]{generate_data()}}.}

\item{op}{Options list with components:
\itemize{
\item \code{useDE} (default \code{TRUE}): include \code{+ De} when
expectancy varies.
\item \code{t_random_slope} (default \code{FALSE}): reserved for
future extension.
\item \code{full_model_out} (default \code{FALSE}): return the full
list including the \code{nlme::lme} fit.
\item \code{simplecarryover} (default \code{FALSE}): add \code{+ tsd} as a
nuisance term when carryover is being modelled linearly.
\item \code{carryover_t1half} (default \code{0}): carryover half-life
used to compute the continuous drug indicator \code{Dbc}.
\item \code{carryover_scalefactor} (default \code{1}): scale factor in
the \code{Dbc} decay.
}}
}
\value{
A tibble with columns \code{beta}, \code{betaSE}, \code{p}, \code{issingular},
\code{warning}. If \code{op$full_model_out = TRUE}, returns a list with
\code{form}, \code{fit}, \code{datamerged}, and the \code{stdout} tibble.
}
\description{
Fits the canonical pmsimstats analysis model via \code{nlme::lme} with
a \code{corCAR1(form = ~t | ptID)} residual correlation and a random
intercept per participant. The fixed-effects formula is assembled
adaptively based on three properties of the input data:
}
\details{
\itemize{
\item \code{var_in_Db}: whether the binary drug indicator varies within
participant. If yes, the formula includes \code{Dbc + bm:Dbc}; if
not, it falls back to \code{bm:t}.
\item \code{var_in_exp}: whether the expectancy vector varies. If yes and
\code{op$useDE = TRUE}, adds \code{+ De}.
\item \code{var_in_tsd}: whether \code{tsd} ever varies. If yes and
\code{op$simplecarryover = TRUE}, adds \code{+ tsd}.
}

Direct port of the pmsimstats-ng tidyverse \code{lme_analysis}.
}
