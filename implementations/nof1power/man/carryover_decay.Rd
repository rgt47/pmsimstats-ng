% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carryover_models.R
\name{carryover_decay}
\alias{carryover_decay}
\title{Residual drug-effect decay multiplier}
\usage{
carryover_decay(
  tsd,
  halflife,
  form = c("exponential", "linear", "weibull", "power"),
  shape = 1,
  power = 1.5
)
}
\arguments{
\item{tsd}{Time since drug discontinuation (same time units as
\code{halflife}). May be a vector.}

\item{halflife}{Carryover half-life. The value of \code{tsd} at which
the multiplier equals 0.5.}

\item{form}{One of \code{"exponential"} (default), \code{"linear"},
\code{"weibull"}, or \code{"power"}.}

\item{shape}{Weibull shape parameter \code{k}. Ignored unless
\code{form = "weibull"}. \code{k = 1} recovers the exponential form;
\code{k < 1} gives a heavier tail; \code{k > 1} gives accelerated washout.}

\item{power}{Power exponent. Ignored unless \code{form = "power"}.}
}
\value{
Numeric vector of the same length as \code{tsd}, with values
in \verb{[0, 1]}.
}
\description{
Returns the fraction of residual drug effect present at
time-since-discontinuation \code{tsd} for a carryover with given
\code{halflife} and functional form. The multiplier equals 1 at
\code{tsd = 0} and 0.5 at \code{tsd = halflife} for all supported forms.
}
\details{
\itemize{
\item Exponential: \verb{phi(t) = exp(-lambda t)} with
\code{lambda = ln(2) / halflife}.
\item Linear: \code{phi(t) = max(0, 1 - t / (2 * halflife))}.
\item Weibull: \verb{phi(t) = exp(-(lambda_w t)^k)} with
\verb{lambda_w = (ln 2)^\{1/k\} / halflife}.
\item Power: \code{phi(t) = max(0, (1 - t / max_time)^power)} with
\code{max_time = 3 * halflife}; supplied as a nof1_power extension.
}
}
\examples{
carryover_decay(c(0, 0.5, 1, 2), halflife = 1)
carryover_decay(c(0, 0.5, 1, 2), halflife = 1, form = "weibull",
                shape = 2)
}
