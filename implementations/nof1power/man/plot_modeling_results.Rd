% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_modeling_results}
\alias{plot_modeling_results}
\title{Plot a power heatmap across a parameter grid}
\usage{
plot_modeling_results(
  data,
  param2plot = "power",
  param2vary,
  param2hold = list(),
  alpha = 0.05
)
}
\arguments{
\item{data}{Either the full list returned by
\code{\link[=generate_simulated_results]{generate_simulated_results()}} (with \verb{$results} and
\verb{$parameterselections}) or just the \code{results} tibble.}

\item{param2plot}{Aggregate quantity; one of \code{"power"},
\code{"mean_beta"}, \code{"mean_betaSE"}, \code{"sd_beta"}, or
\code{"mean_frac_NA"}.}

\item{param2vary}{Character vector of length 4 naming the
varying parameters (columns of the results tibble).}

\item{param2hold}{Named list of parameters to hold fixed.}

\item{alpha}{Significance threshold for the \code{"power"} metric.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a \code{ggplot2} heatmap from the \code{results} tibble returned
by \code{\link[=generate_simulated_results]{generate_simulated_results()}}. Rows of \code{results} are
per-replicate analysis outputs with columns \code{trialdesign},
\code{respparamset}, \code{blparamset}, \code{modelparamset}, parameters from
\code{modelparams} (typically \code{N}, \code{c.bm}, \code{carryover_t1half}), and
replicate-level quantities \code{beta}, \code{betaSE}, \code{p}, \code{irep}.
}
\details{
The caller specifies four varying parameters (\code{param2vary}),
parameters to hold fixed (\code{param2hold}), and which aggregate
quantity to plot (\code{param2plot}). The first two \code{param2vary}
entries form the facet grid; the last two form the tile x and y
axes. Aggregation is computed with \code{dplyr::group_by} over the
four varying parameters.
}
