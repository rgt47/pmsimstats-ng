% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{generate_simulated_results}
\alias{generate_simulated_results}
\title{Orchestrate simulation over a parameter grid}
\usage{
generate_simulated_results(
  trialdesigns,
  respparamsets,
  blparamsets,
  censorparams,
  modelparams,
  simparam,
  analysisparams,
  rawdataout = FALSE,
  lambda_cor = NA,
  n_cores = 1,
  dgp_architecture = "mvn"
)
}
\arguments{
\item{trialdesigns}{List of trial-design objects, each the output
of \code{\link[=build_trial_design]{build_trial_design()}} (a \code{list(metadata, trialpaths)}).}

\item{respparamsets}{List of response-parameter wrappers, each a
list with element \code{param} that is a tibble accepted by
\code{\link[=generate_data]{generate_data()}}.}

\item{blparamsets}{List of baseline-parameter wrappers, each a
list with element \code{param} that is a tibble accepted by
\code{\link[=generate_data]{generate_data()}}.}

\item{censorparams}{Either \code{NA} (no censoring) or a data frame of
censoring parameters accepted by \code{\link[=censor_data]{censor_data()}}.}

\item{modelparams}{Data frame of model parameters, one row per
parameter set.}

\item{simparam}{List with simulation controls:
\itemize{
\item \code{Nreps}: number of replicates per parameter set.
\item \code{progressiveSave}: logical, save in chunks.
\item \code{basesavename}: file prefix.
\item \code{nRep2save}: chunk size.
\item \code{saveunit2start}: chunk to start from.
\item \code{savedir}: directory for saved files.
}}

\item{analysisparams}{Data frame of analysis options (one row per
analysis variant), each with columns matching the \code{op} list in
\code{\link[=lme_analysis]{lme_analysis()}}.}

\item{rawdataout}{Logical; return raw simulated data.}

\item{lambda_cor}{Correlation decay rate; see \code{\link[=generate_data]{generate_data()}}.}

\item{n_cores}{Number of parallel workers; \verb{< 1} auto-detects.
Currently sequential; reserved for future \code{furrr} integration.}

\item{dgp_architecture}{Either \code{"mvn"} or \code{"mean_moderation"}.}
}
\value{
A list with elements \code{results} (tibble of per-replicate
beta/SE/p) and \code{parameterselections} (record of inputs).
}
\description{
Iterates over the Cartesian product of trial designs, response
parameter sets, baseline parameter sets, and model parameter rows;
caches sigma matrices per (design, path) combination; and runs
\code{\link[=generate_data]{generate_data()}} plus \code{\link[=lme_analysis]{lme_analysis()}} for each cell, optionally
with and without censoring. Direct port of the pmsimstats-ng
tidyverse \code{generate_simulated_results}.
}
