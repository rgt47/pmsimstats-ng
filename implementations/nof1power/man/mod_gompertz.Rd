% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/math_utilities.R
\name{mod_gompertz}
\alias{mod_gompertz}
\title{Modified Gompertz response curve}
\usage{
mod_gompertz(t, maxr, disp, rate)
}
\arguments{
\item{t}{Numeric time vector (typically \code{t_wk_cumulative}, \code{tpb},
or \code{tod}).}

\item{maxr}{Asymptotic maximum of the response curve.}

\item{disp}{Displacement parameter controlling curve shape.}

\item{rate}{Rate parameter controlling curve steepness.}
}
\value{
Numeric vector of the same length as \code{t}. \code{y(0) = 0} and
\code{y(t) -> maxr} as \code{t -> Inf}.
}
\description{
Origin-passing Gompertz curve used to describe each latent factor
(time-variant, pharm-biomarker, bio-response) as a function of
cumulative time in the relevant phase. The implementation matches
the corrected Hendrickson formulation adopted in the pmsimstats-ng
tidyverse reference.
}
\details{
The curve is constructed from the base Gompertz form
\code{maxr * exp(-disp * exp(-rate * t))} with a vertical offset
subtraction so the curve passes through the origin, then rescaled
to preserve the asymptote \code{maxr}.
}
\examples{
mod_gompertz(seq(0, 10, by = 1), maxr = 10, disp = 1, rate = 0.5)
}
